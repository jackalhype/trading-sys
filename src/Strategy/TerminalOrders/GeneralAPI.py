from typing import Tuple

class GeneralAPI:
    @staticmethod
    def get_current_price(ticker: str) -> Tuple[float, float]:
        """
        TODO:
        """
        bid = 0.0
        ask = 0.0
        return (bid, ask)
